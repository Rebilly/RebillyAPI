type: object
allOf:
  - $ref: "#/components/schemas/InvoiceItemInline"
  - properties:
      _links:
        type: array
        description: The links related to resource
        readOnly: true
        minItems: 1
        items:
          anyOf:
            - $ref: "#/components/schemas/SelfLink"
            - $ref: "#/components/schemas/SubscriptionLink"