type: object
properties:
  invoiceId:
    $ref: "#/components/schemas/ResourceId"
  transactionId:
    $ref: "#/components/schemas/ResourceId"
  amount:
    type: number
  currency:
    allOf:
      - $ref: "#/components/schemas/CurrencyCode"
