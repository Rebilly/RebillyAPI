content:
  application/json:
    schema:
      oneOf:
        - $ref: "#/components/schemas/one-time-order"
        - $ref: "#/components/schemas/subscription-order"
description: Subscription resource
required: true
