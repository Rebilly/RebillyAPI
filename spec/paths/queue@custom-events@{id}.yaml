parameters:
  - $ref: "#/parameters/resourceId"
get:
  tags:
    - Custom Events
  summary: Retrieve a scheduled custom event
  description: |
    Retrieve a scheduled custom event with predefined identifier string
  responses:
    200:
      description: Scheduled custom event was retrieved successfully
      headers:
        X-Rate-Limit-Limit:
          $ref: "#/headers/X-Rate-Limit-Limit"
        X-Rate-Limit-Remaining:
          $ref: "#/headers/X-Rate-Limit-Remaining"
        X-Rate-Limit-Reset:
          $ref: "#/headers/X-Rate-Limit-Reset"
      schema:
        $ref: "#/definitions/CustomEvent"
    303:
      description: Custom event was successfully processed and moved out from queue
      headers:
        X-Rate-Limit-Limit:
          $ref: "#/headers/X-Rate-Limit-Limit"
        X-Rate-Limit-Remaining:
          $ref: "#/headers/X-Rate-Limit-Remaining"
        X-Rate-Limit-Reset:
          $ref: "#/headers/X-Rate-Limit-Reset"
      schema:
        $ref: "#/definitions/CustomEvent"
    401:
      $ref: "#/responses/AccessForbidden"
    404:
      $ref: "#/responses/NotFound"
put:
  tags:
    - Custom Events
  summary: Update a scheduled custom event with predefined ID
  description: |
    Update a scheduled custom event with predefined identifier string
  parameters:
    - name: body
      in: body
      description: Custom event resource
      required: true
      schema:
        $ref: "#/definitions/CustomEvent"
  responses:
    200:
      description: Scheduled custom event was updated
      headers:
        X-Rate-Limit-Limit:
          $ref: "#/headers/X-Rate-Limit-Limit"
        X-Rate-Limit-Remaining:
          $ref: "#/headers/X-Rate-Limit-Remaining"
        X-Rate-Limit-Reset:
          $ref: "#/headers/X-Rate-Limit-Reset"
      schema:
        $ref: "#/definitions/CustomEvent"
    401:
      $ref: "#/responses/AccessForbidden"
    404:
      $ref: "#/responses/NotFound"
    422:
      $ref: "#/responses/InvalidDataError"
delete:
  tags:
    - Custom Events
  summary: Delete a scheduled custom event
  description: |
    Delete a scheduled custom event with predefined identifier string
  responses:
    204:
      headers:
        X-Rate-Limit-Limit:
          $ref: "#/headers/X-Rate-Limit-Limit"
        X-Rate-Limit-Remaining:
          $ref: "#/headers/X-Rate-Limit-Remaining"
        X-Rate-Limit-Reset:
          $ref: "#/headers/X-Rate-Limit-Reset"
      description: Scheduled custom event was deleted
    401:
      $ref: "#/responses/AccessForbidden"
    404:
      $ref: "#/responses/NotFound"
