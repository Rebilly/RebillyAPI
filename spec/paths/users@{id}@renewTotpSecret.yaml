parameters:
  - $ref: "#/parameters/resourceId"
  - name: token
    in: path
    description: The token string
    type: string
    required: true
post:
  tags:
    - Users
  summary: Renew totpSecret
  description: |
    Renew totpSecret
  parameters:
  responses:
    201:
      description: totpSecret renewed successfully
      headers:
        Rate-Limit-Limit:
          $ref: "#/headers/Rate-Limit-Limit"
        Rate-Limit-Remaining:
          $ref: "#/headers/Rate-Limit-Remaining"
        Rate-Limit-Reset:
          $ref: "#/headers/Rate-Limit-Reset"
      schema:
        $ref: "#/definitions/User"
    404:
      $ref: "#/responses/NotFound"
