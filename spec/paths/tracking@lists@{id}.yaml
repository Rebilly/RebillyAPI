parameters:
  - $ref: "#/parameters/resourceId"
get:
  tags:
    - Tracking
  summary: Retrieve List's changes history
  responses:
    200:
      description: List's changes history was retrieved successfully
      schema:
        type: array
        items:
          $ref: "#/definitions/List"
    404:
      $ref: "#/responses/NotFound"
    401:
      $ref: "#/responses/AccessForbidden"
