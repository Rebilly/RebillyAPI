type: object
required:
  - expiredTime
properties:
  expiredTime:
    description: The coupon's expiry time
    type: string
    format: date-time
  reason:
    description: The coupon's expiry time
    type: string
    format: date-time
