type: object
discriminator: operation
properties:
  operation:
    type: string
    enum:
      # TODO: Consider to use `not` instead of `not-*`
      - and
      - or
      - not
      - between
      - equals
      - in
      - greater-than
      - less-than
required:
  - operation
