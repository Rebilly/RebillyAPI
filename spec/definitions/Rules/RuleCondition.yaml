type: object
discriminator: operation
properties:
  operation:
    type: string
    enum:
      # TODO: Consider to use `not` instead of `not-*`
      - and
      - or
      - compare
      - between
      - not-between
      - in
      - not-in
required:
  - operation
