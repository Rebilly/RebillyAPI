type: object
description: In condition
allOf:
  - $ref: "#/definitions/RuleCondition"
  - type: object
    properties:
      field:
        type: string
      values:
        type: array
        items:
          type: string
    required:
      - field
      - values
