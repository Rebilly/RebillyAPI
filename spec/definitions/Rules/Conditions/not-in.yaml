type: object
description: Not in condition
allOf:
  - $ref: "#/definitions/RuleCondition"
  - type: object
    properties:
      field:
        type: string
      values:
        type: array
        items:
          type: string
      strict:
        type: boolean
        default: true
    required:
      - field
      - values
