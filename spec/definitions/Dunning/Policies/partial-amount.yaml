allOf:
  - $ref: "#/definitions/AmountAdjustment"
  - type: object
    properties:
      value:
        description: The payment amount (accepted number or percentage)
        type: string
    required:
      - value
