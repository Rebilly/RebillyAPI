type: object
discriminator: rel
properties:
  rel:
    description: The relationship name
    type: string
    enum:
      - "self"
      - "notes"
      - "defaultCard"
      - "defaultPaymentInstrument"
required:
  - rel
