type: object
required:
  - method
discriminator: "method"
properties:
  method:
    description: The payment method
    type: string
    enum:
      - ach
      - cash
      - payment-card
      - paypal
      - china-unionpay
      - OchaPay
      - WeChat Pay
