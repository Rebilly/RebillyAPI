description: Rotessa config
allOf:
  -
    $ref: "#/definitions/GatewayAccount"
  -
    type: "object"
    required:
     - "gatewayConfig"
    properties:
      gatewayConfig:
        type: object
        description: Rotessa credentials object
        properties:
          apiKey:
            type: string
            description: Rotessa merchant ID
            format: password
        required:
          - "apiKey"
