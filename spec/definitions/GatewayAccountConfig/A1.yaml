description: A1 Gateway
allOf:
  -
    $ref: "#/definitions/GatewayAccount"
  -
    type: "object"
    properties:
      gatewayConfig:
        type: object
        properties:
          accountId:
            type: string
          password:
            type: string
            format: password
    required:
      - "accountId"
      - "password"
