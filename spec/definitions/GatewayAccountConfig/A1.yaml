description: A1 Gateway
    $ref: "#/definitions/GatewayAccount"
  -
    type: "object"
    properties:
      accountId:
        type: string
      password:
        type: string
        format: password
    required:
      - "accountId"
      - "password"
