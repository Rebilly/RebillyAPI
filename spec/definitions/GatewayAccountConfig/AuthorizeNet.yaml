description: AuthorizeNet Gateway
allOf:
  -
    $ref: "#/definitions/GatewayAccount"
  -
    type: "object"
    properties:
      gatewayConfig:
        type: object
        properties:
          apiLoginId:
            type: string
          transactionKey:
            type: string
            format: password
    required:
      - "apiLoginId"
      - "transactionKey"
